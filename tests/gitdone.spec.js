'use strict';

let chai = require('chai');
let path = require('path');
chai.should();

let Gitdone = require(path.join(__dirname, '..', 'gitdone'));

describe('Gitdone class', () => {
  describe('constructor', () => {});
  describe('modifyUI', () => {});
  describe('addElements', () => {});
  describe('renderToolbarUI', () => {});
});
